services:
  portfolio:
    build: .
    container_name: portfolio-site
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:80"
    volumes:
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config:
